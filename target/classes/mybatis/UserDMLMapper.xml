<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
<mapper namespace="UserDMLMapper">
	
	<insert id="userJoin" parameterType="Hashmap"> 
        INSERT INTO TB_MILKOMEDA_USER 
        ( USER_ID, 
        USER_PASSWORD, 
        USER_EMAIL, 
        USER_CHK_EMAIL,
        USER_ACCESS,
        USER_NICKNAME,
		USER_AGE,
		USER_NAME,
		USER_PHONE,
		USER_PROFILE_IMAGE,
		USER_GENDER,
		USER_NATION,
		USER_ADDRESS
        ) 
        VALUES 
        ( #{userId, jdbcType=NVARCHAR},
          #{userPassword, jdbcType=NVARCHAR},
          #{userEmail, jdbcType=NVARCHAR},
          #{userChkEmail, jdbcType=NVARCHAR}, 
          #{userAccess, jdbcType=NVARCHAR},
          #{userNickname, jdbcType=NVARCHAR},
          #{userAge, jdbcType=NVARCHAR},
          #{userName, jdbcType=NVARCHAR},
          #{userPhone, jdbcType=NVARCHAR},
          #{userProfileImage, jdbcType=NVARCHAR},
          #{userGender, jdbcType=NVARCHAR},
          #{userNation, jdbcType=NVARCHAR},
          #{userAddress, jdbcType=NVARCHAR}) 
    </insert>
    
    <update id="userUpdate" parameterType="Hashmap">
		UPDATE TB_MILKOMEDA_USER 	
		SET 
		USER_PASSWORD =#{userPassword},
		USER_NICKNAME = #{userNickname},
		USER_AGE = #{userAge},
		USER_NAME = #{userName},
		USER_PHONE = #{userPhone},
		USER_PROFILE_IMAGE = #{userProfileImage},
		USER_GENDER = #{userGender},
		USER_NATION = #{userNation},
		USER_ADDRESS = #{userAddress}	
		WHERE USER_ID = #{userId} and USER_Email=#{userEmail}
	</update>
	
	<delete id="userDelete" parameterType="Hashmap">
		DELETE FROM TB_MILKOMEDA_USER
		WHERE USER_ID = #{userId} and USER_EMAIL=#{userEmail}	
	</delete>

</mapper>